/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["uploads.mangadex.org"], // Add the Mangadex domain here
  },
};

export default nextConfig; // Use export default for ES modules
